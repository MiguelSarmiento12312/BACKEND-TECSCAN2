import dotenv from 'dotenv';
dotenv.config();  // Esto debe estar al principio

const config = {
    baseUrl: 'https://e27c-76-137-84-85.ngrok-free.app' // Asignamos la URL de ngrok directamente
};

export default config; // Exportación correcta para ES Modules
